#include<stdio.h>
int factorial(int){
    int i;
     for(i=1;i<=number;i++){
      fact=fact*i;
      return fact;
}
int main()
{
 int i,fact=1,number;
 printf("Enter a number:");
  scanf("%d",&number);
   int L = int factorial(fact);
  }
  printf("Factorial of %d is: %d",L);
return 0;
}
